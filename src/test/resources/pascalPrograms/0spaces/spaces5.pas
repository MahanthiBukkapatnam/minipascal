program      exam1ple3with4numbers (input,output);
var x,y:integer;
function gcd(a,b:integer):integer;
begin{gcd}
if b=then gcd:=a else gcd:=(b,a mod b)
end;{gcd}
begin{example}
read(x,y);
write(gcd(x,y));
write('Mahanthi');
write('a');
write('b');
id = 0

end
